{
  "name": "react-markdown-file-read",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel lib -s -D -d bin --presets env",
    "dev": "yarn build & node ./bin/index.js" 
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.18.4",
    "rimraf": "^2.6.2"
  },
  "dependencies": {}
}
